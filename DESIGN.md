# Визуальный мир: тёмная студия

Мир закреплён владельцем по образцу дашборда академии: **почти чёрная тёплая
основа и одно тёплое золото**. Выбор не косметический — панель весь день
показывает чужие кадры, и светлый интерфейс с ними спорит. Тёмная основа
отдаёт единственное яркое пятно содержимому: миниатюре поста.

## Палитра

Все значения — токены в `public/css/tokens.css`. За их пределами цветов нет.

| Роль | Токен | Значение |
|---|---|---|
| Основа | `--bg` | `#0B0908` — тёплый почти-чёрный |
| Панель | `--surface` | `#141110` |
| Приподнятое (поля, активный пункт) | `--surface-2` | `#1B1613` |
| Наведение | `--surface-3` | `#221C18` |
| Граница | `--line` | `#2C2420` |
| Основной текст | `--ink` | `#EFE8DE` — тёплый белый, не чистый |
| Второй уровень | `--ink-2` | `#A79B8C` |
| Микро-подписи | `--ink-3` | `#7E7263` |
| Акцент | `--gold` | `#E0A94B` |
| Успех / внимание / беда | `--ok` `--warn` `--danger` | `#56BE80` `#E0A63C` `#E2604F` |

**Золото тратится только на три вещи:** главное действие, текущий выбор,
индикатор состояния. Как только оно становится украшением — мир рассыпается.

Серый для вторичного текста берётся тёплым (подмешан к основе), а не
нейтральным: холодный серый на тёплом чёрном читается грязью.

## Типографика

Одно семейство на всё. Продуктовый интерфейс не нуждается в паре
«дисплей + текст»: заголовки, кнопки, подписи и данные держит один хорошо
настроенный гротеск.

Шкала фиксированная, шаг ~1.2 — не плавающая: панель смотрят на постоянном
DPI, и заголовок, ужимающийся в узкой колонке, выглядит хуже, а не лучше.

`11 · 12 · 13 · 15 · 18 · 22 · 30` px. Цифры данных — табличные (`tnum`),
иначе колонки времени и счётчиков пляшут.

Микро-подписи разделов — 11px, верхний регистр, разрядка `.08em`, `--ink-3`.

## Формы и глубина

- Радиусы: `8px` контролы, `12px` карточки, `999px` пилюли статуса.
- Тень всегда со смещением и мягким размытием. Цветное свечение без смещения —
  украшение, его нет.
- Граница `1px`. **Толстой цветной полосы слева у карточек нет** — статус несёт
  точка и подпись, а не трёхпиксельный кант.

## Иконки

Рисованные SVG в одном штрихе `1.6px`, сетка 24. Эмодзи и юникодные значки
интерфейсом не используются — только в тексте пользователя.

## Движение

150–200 мс на смену состояния. Движение сообщает изменение: появление панели,
переход дня, ответ кнопки. Хореографии загрузки нет — панель открывается в
задачу, а не в представление.

## Плотность и отклик

Экран — рабочий стол, планшет и телефон, потому что смотреть расписание будут
и с дивана.

- ≥1200px: боковая навигация с подписями.
- 900–1200px: рельс с одними иконками.
- <900px: навигация уезжает в выдвижную панель, композер становится одной
  лентой — форма, потом превью.

## Состояния

У каждого интерактивного элемента есть: обычное, наведение, фокус с видимым
контуром, нажатие, запрет, загрузка. Пустые экраны объясняют, что сделать,
а не сообщают «пусто».
