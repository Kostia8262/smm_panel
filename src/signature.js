/**
 * Подпись к постам — тонкий реэкспорт.
 *
 * Реализация лежит в `public/js/signature.js`, потому что тот же код нужен
 * браузеру: счётчик символов в композере, превью и отправка обязаны считать
 * один и тот же текст. Две копии этой функции разошлись бы через неделю, и
 * пост, прошедший проверку в панели, отвалился бы у Threads на 500 знаках.
 */

export { withSignature, signatureFor } from '../public/js/signature.js';
