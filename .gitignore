node_modules/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
# В data/ лежат база, загруженные файлы, токен владельца и КЛЮЧ ШИФРОВАНИЯ
# доступов к соцсетям. Перечислять по одному оказалось ошибкой: secret.key
# под маску не попал и чуть не уехал в публичный репозиторий. Закрываем
# каталог целиком — ничего, что должно быть в git, там не появляется.
data/
.env
*.log
.DS_Store

# Личные настройки Claude Code: там разрешения на ssh к боевому серверу.
# Репозиторий публичный — адресу VPS в нём не место.
.claude/settings.local.json
